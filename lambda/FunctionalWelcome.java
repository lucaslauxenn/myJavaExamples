@FunctionalInterface
public interface FunctionalWelcome {
    void welcome();
}

// Notice that functional classes can only have one method,
// so the class implementing it doesn't need to specify which method to override.